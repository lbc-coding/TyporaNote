# Flutter_Widget_Document



## 一、Widget

​	在flutter中，一切皆组件。

​	组件有多种属性、方法。

​	组件可嵌套。

### １、主要用途

​	widget是最基础的组件，它是其他组件的根类。<u>*PS：在Dart语言中，一切皆对象，Object是所有对象的根类。*</u>

### ２、继承的Flutter的何种组件类型

​	它是组件的根类。

​	分有状态组件(**StatefulWidget**)和无状态组件(**StatelessWidget**)。

​	可以重写Widget的build方法来构建一个组件。

​	例如：

​	＠override

​	 Widget build(BuildContext context);

### ３、属性及描述

| 属性名 | 类型 | 说明 |
| :----: | :--: | :--: |
|        |      |      |
|        |      |      |
|        |      |      |

